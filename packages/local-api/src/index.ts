export const serve = (dir: string, filename: string, port: number) => {
  console.log('serving traffic on port', port);
  console.log('saving/fetching cells from', filename);
  console.log('file is located in directory:', dir);
};